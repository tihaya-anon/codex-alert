[project]
name = "scripts"
version = "0.1.0"
description = "Utilities for Codex hook assets and local overlay development"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cairosvg>=2.9.0",
]
